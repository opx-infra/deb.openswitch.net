# OpenSwitch Debian Package Repository

*Aptly publishing to S3*

Initial setup of remote state

```bash
cd remote-state
terraform init
terraform apply
cd ..
terraform init
```

Apply changes normally

```bash
terraform plan
terraform apply
```
